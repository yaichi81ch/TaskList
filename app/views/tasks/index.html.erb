<h1>タスクリスト</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %>:<%= task.content %>'→期限:'<%= task.duedate %>(<%= task.status %>)</li>
  <% end %>
</ul>

<%= link_to '新規タスクの追加', new_task_path %>

  
