<table class="table table-striped table-bordered">
  <thead>
    <tr>
      <th>Task</th>
      <th>Due</th>
      <th>Status</th>
    </tr>
  </thead>
  <tbody>
    <%= form_for(task) do |f| %>
    <tr>
      <td><%= f.text_field :content %></td>
      <td><%= f.date_field :duedate %></td>
      <td><%= f.select :status, ["未着手","仕掛中","完了"] %></td>
    </tr>
    <tr><td colspan="3" style="text-align:right;"><%= f.submit 'Add!' %></td></tr>
    <% end %>
  </tbody>
</table>